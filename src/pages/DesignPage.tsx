import CreateDesign from "../components/editor/CreateDesign";
import Layout from "../layout";

const DesignPage = () => {
  return (
    <Layout>
      <CreateDesign />
    </Layout>
  );
};

export default DesignPage;
